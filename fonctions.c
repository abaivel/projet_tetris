#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "fonctions.h"

typedef struct {
	char** form;
	int nb_orient;
}Piece

void show(char** grille){
}

void createPiece(Piece* pieces){
}

void saveScore(int score, char pseudo[]){
}

void recup_Score(){
}

void clear_scan(){
	char c;
	int res;
	do{
		res = fscan
}

char getChar()
{
    int c = 0;
	c = getc(stdin);
    while (getc(stdin) != '\n' && getc(stdin) != EOF);
    return c;
}



